#cloud-config
# Build-time seed for the qemu builder only (gives Packer a known ubuntu/ubuntu
# login). Throwaway: no identity is baked in, and cleanup.sh resets it.
password: ubuntu
ssh_pwauth: true
chpasswd:
  expire: false

# Locale / keyboard / timezone defaults (overridable at clone time).
locale: nl_BE.UTF-8
keyboard:
  layout: be
timezone: Europe/Brussels

# Let cloud-init manage the hostname (generic; real hostname set at clone time).
preserve_hostname: false

# Update packages on first boot.
package_update: true
package_upgrade: true

# Minimal generic packages. qemu-guest-agent is required so Proxmox can see the
# VM's IP. Extra tooling (zsh, kubectl, ...) comes from the PDS package in issue #3.
packages:
  - qemu-guest-agent
  - curl
  - file
